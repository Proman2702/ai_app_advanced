import 'package:flutter/material.dart';

class MainShellScaffold extends StatelessWidget {
  final String title;
  final Widget child;
  const MainShellScaffold({required this.title, required this.child, super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
