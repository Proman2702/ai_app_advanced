import 'package:flutter/material.dart';

class DiagnosticsMenuScreen extends StatelessWidget {
  const DiagnosticsMenuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
